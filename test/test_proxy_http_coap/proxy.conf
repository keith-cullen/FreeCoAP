port = "12437"
max_log_level = "debug"

[http_server]
trust_file = "root_client_cert.pem"
cert_file = "server_cert.pem"
key_file = "server_privkey.pem"

[coap_client]
trust_file = "root_server_cert.pem"
cert_file = "client_cert.pem"
key_file = "client_privkey.pem"
